services:
  frontend:
    build: .
    container_name: frontend_desrr
    hostname: frontend
    command: npm run dev
    volumes:
      - ./frontend:/app
      - /app/node_modules

    ports:
      - "3000:3000"
    environment:
      - CHOKIDAR_USEPOLLING=true

